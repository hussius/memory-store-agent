[project]
name = "memory-store-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.107.0",
    "python-dotenv>=1.0.0",
    "discord-py>=2.4.0",
]
